define(function(){
    var chartActions = (function(){
        this.actions = {
            ADD: null,       // Adds widget to dashboard
            REMOVE: null    // Removes widget from dashboard
        }
    });
});
